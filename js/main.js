// Inicialización general
document.addEventListener('DOMContentLoaded', () => {
  // Incluir toggle de tema en header si se necesita
  // El componente header ya debe tener un botón con id="theme-toggle"
});
